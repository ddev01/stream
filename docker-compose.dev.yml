x-development-network: &development-network
  - development

x-laravel-dev-volumes: &laravel-dev-volumes
  - .:/var/www/html/

x-laravel-dev-base: &laravel-dev-base
  build:
    target: development
    args:
      USER_ID: ${SPIN_USER_ID}
      GROUP_ID: ${SPIN_GROUP_ID}
  volumes: *laravel-dev-volumes
  networks: *development-network

services:
  traefik:
    ports:
      - "80:80"
      - "443:443"
    networks: *development-network
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./.infrastructure/conf/traefik/dev/traefik.yml:/traefik.yml:ro
      - ./.infrastructure/conf/traefik/dev/traefik-certs.yml:/traefik-certs.yml
      - ./.infrastructure/conf/traefik/dev/certificates/:/certificates

  php:
    <<: *laravel-dev-base
    depends_on:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.laravel.rule=Host(`localhost`) || HostRegexp(`.*\\.ngrok-free\\.dev`)"
      - "traefik.http.routers.laravel.entrypoints=web"
      - "traefik.http.services.laravel.loadbalancer.server.port=8080"
      - "traefik.http.services.laravel.loadbalancer.server.scheme=http"

  node:
    image: node:22
    volumes: *laravel-dev-volumes
    working_dir: /usr/src/app/
    networks: *development-network

  mailpit:
    image: axllent/mailpit
    networks: *development-network
    ports:
      - "8025:8025"

  postgres:
    image: postgres:16-alpine
    networks: *development-network
    environment:
      POSTGRES_DB: "${DB_DATABASE:-laravel}"
      POSTGRES_USER: "${DB_USERNAME:-laravel}"
      POSTGRES_PASSWORD: "${DB_PASSWORD:-password}"
      POSTGRES_HOST_AUTH_METHOD: "scram-sha-256"
    ports:
      - target: 5432
        published: 5432
        mode: host
    volumes:
      - ./.infrastructure/volume_data/postgres/database_data/:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-laravel}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  redis:
    command: "redis-server --appendonly yes --requirepass redispassword"
    volumes:
      - ./.infrastructure/volume_data/redis/data:/data
    networks: *development-network

  ssh:
    image: serversideup/docker-ssh:v2.0.0
    ports:
      - "2222:2222"
    environment:
      AUTHORIZED_KEYS: >
        "# Start Keys
         ssh-ed25519 1234567890abcdefghijklmnoqrstuvwxyz user-a
         ssh-ed25519 abcdefghijklmnoqrstuvwxyz1234567890 user-b
         # End Keys"
      ALLOWED_IPS: "AllowUsers tunnel"
    volumes:
      - tunnel_ssh_host_keys_dev:/etc/ssh/ssh_host_keys
    networks: *development-network
    depends_on:
      postgres:
        condition: service_healthy

  horizon:
    <<: *laravel-dev-base
    command: ["php", "/var/www/html/artisan", "horizon"]
    depends_on:
      redis:
        condition: service_healthy
      php:
        condition: service_started
    healthcheck:
      test: ["CMD", "php", "/var/www/html/artisan", "horizon:status"]
      interval: 1m30s
      timeout: 10s
      retries: 3
      start_period: 40s
      start_interval: 5s

volumes:
  tunnel_ssh_host_keys_dev:

networks:
  development:
